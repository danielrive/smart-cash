### Rule Description
##  Allow communication from expenses to Frontend svc pods

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: expenses-frontend
spec:
  podSelector:
    matchLabels:
      app: expenses
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          ingress: nginx
    ports:
    - protocol: TCP
      port: 80
  egress:
  - to:
    - podSelector:
        matchLabels:
          ingress: nginx